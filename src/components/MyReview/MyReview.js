import React from 'react';
import useTitle from '../useTitle/useTitle';

const MyReview = () => {
    useTitle('myreview')
    return (
        <div>
            <h1>My Review</h1>
        </div>
    );
};

export default MyReview;